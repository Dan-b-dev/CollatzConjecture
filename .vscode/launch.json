{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Collatz",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/collatz.exe",
            "cwd": "${workspaceFolder}",
            "args": [],
            "stopAtEntry": false,
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "preLaunchTask": "Build with Make"
        }
    ]
}